import React from "react";
import ReactDOM from "react-dom/client";
import DriverHOSTracker from "./driver_hos_tracker_web_app.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DriverHOSTracker />
  </React.StrictMode>
);
